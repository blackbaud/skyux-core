name: SKY UX Pipeline

on:
  pull_request:
    branches: [
      master
    ]
  push:
    branches: [
      master
    ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '10.x'
      - run: npm install --ignore-scripts
      - run: npm install --no-save blackbaud/skyux-sdk-builder-config
      - name: List SKY UX versions
        run: npx -p @skyux-sdk/cli@^4.0.0-rc.0 skyux version
      - name: Install SSL certificates
        run: npx -p @skyux-sdk/cli@^4.0.0-rc.0 skyux certs install
      - name: Build SPA
        run: npx -p @skyux-sdk/cli@^4.0.0-rc.0 skyux build
      - name: Build library
        run: npx -p @skyux-sdk/cli@^4.0.0-rc.0 skyux build-public-library
  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '10.x'
      - run: npm install --ignore-scripts
      - run: npm install --no-save blackbaud/skyux-sdk-builder-config
      - name: Run code coverage
        run: npx -p @skyux-sdk/cli@^4.0.0-rc.0 skyux test --coverage library --platform travis
        env:
          BROWSER_STACK_ACCESS_KEY: ${{ secrets.BROWSER_STACK_ACCESS_KEY }}
          BROWSER_STACK_USERNAME: ${{ secrets.BROWSER_STACK_USERNAME }}
          BROWSER_STACK_PROJECT: ${{ github.repository }}
          TRAVIS_BUILD_NUMBER: ${{ github.run_number }}
      - name: Publish coverage report
        run: bash <(curl -s https://codecov.io/bash)
        if: job.status == 'success'
  visual:
    name: Visual tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '10.x'
      - run: npm install --ignore-scripts
      - run: npm install --no-save blackbaud/skyux-sdk-builder-config
      - name: Run visual tests
        run: npx -p @skyux-sdk/cli@^4.0.0-rc.0 skyux e2e --platform travis
        env:
          BROWSER_STACK_ACCESS_KEY: ${{ secrets.BROWSER_STACK_ACCESS_KEY }}
          BROWSER_STACK_USERNAME: ${{ secrets.BROWSER_STACK_USERNAME }}
          BROWSER_STACK_PROJECT: ${{ github.repository }}
          TRAVIS_BUILD_NUMBER: ${{ github.run_number }}