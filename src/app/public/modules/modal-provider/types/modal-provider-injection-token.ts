import {
  InjectionToken
} from '@angular/core';

import {
  SkyModalProvider
} from './modal-provider';

export const SKY_MODAL_PROVIDER = new InjectionToken<SkyModalProvider>('SKY_MODAL_PROVIDER');
